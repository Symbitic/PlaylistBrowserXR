{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start"
  },
  "dependencies": {
    "@azure/functions": "^1.2.3",
    "dotenv": "^10.0.0",
    "node-fetch": "^2.6.1",
    "querystring": "^0.2.1"
  },
  "devDependencies": {
    "@types/node-fetch": "^2.5.10",
    "typescript": "^4.0.0"
  }
}
